def main(a,b):
    """
    Return zero if the numbers are equal, return the larger one if they are not equal.
    Args:
        a: First number.
        b: Second number.
    Returns:
        int: return answer.
    """
    asd = a
    
    if a > b:
        asd = a
    if a < b:
        ads = b
    if a == b:
        ads = 0
    return ads

print(main(3,7))
    